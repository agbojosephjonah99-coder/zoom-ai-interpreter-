{
  "name": "zoom-interpreter-bot",
  "version": "1.0.0",
  "description": "AI-powered Zoom interpreter bot — EN → FR via Recall.ai + OpenAI (Whisper + GPT-4o + TTS)",
  "main": "src/bot.js",
  "scripts": {
    "start": "node src/bot.js",
    "dev": "nodemon src/bot.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
